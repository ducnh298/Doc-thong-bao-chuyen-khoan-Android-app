# ADB Test Commands - Giả lập thông báo chuyển khoản
#
# Dùng am broadcast → DebugReceiver (chỉ hoạt động với bản debug)
# Cài app: adb install -r app/build/outputs/apk/debug/app-debug.apk
# Xem log: adb logcat -s DebugReceiver MainActivity

# ── MBBank ───────────────────────────────────────────────────────────────────
adb shell 'am broadcast -n com.app.docthongbaochuyenkhoan/.service.DebugReceiver --es pkg com.mbmobile --es title "MB Bank +500,000 VND" --es text "TK:1234567890 +500,000VND SD:2,000,000VND"'

adb shell 'am broadcast -n com.app.docthongbaochuyenkhoan/.service.DebugReceiver --es pkg com.mbmobile --es title "MB Bank -200,000 VND" --es text "TK:1234567890 -200,000VND SD:1,800,000VND"'

adb shell 'am broadcast -n com.app.docthongbaochuyenkhoan/.service.DebugReceiver --es pkg com.mbmobile --es title "MB Bank +10,500,000 VND" --es text "TK:1234567890 +10,500,000VND SD:50,000,000VND"'

# ── Vietcombank ───────────────────────────────────────────────────────────────
adb shell 'am broadcast -n com.app.docthongbaochuyenkhoan/.service.DebugReceiver --es pkg com.VCB --es title "VCB +1,200,000 VND" --es text "TK:0011234567 +1,200,000VND SD:5,000,000VND"'

adb shell 'am broadcast -n com.app.docthongbaochuyenkhoan/.service.DebugReceiver --es pkg com.VCB --es title "VCB -500,000 VND" --es text "TK:0011234567 -500,000VND SD:4,500,000VND"'

# ── Techcombank ───────────────────────────────────────────────────────────────
adb shell 'am broadcast -n com.app.docthongbaochuyenkhoan/.service.DebugReceiver --es pkg com.techcombank.mb.android --es title "TCB +350,000 VND" --es text "TK:9876543210 +350,000VND SD:3,000,000VND"'

# ── Agribank ──────────────────────────────────────────────────────────────────
adb shell 'am broadcast -n com.app.docthongbaochuyenkhoan/.service.DebugReceiver --es pkg com.Agribank3g --es title "Agribank +2,500,000 VND" --es text "TK:12345678901 +2,500,000VND SD:10,000,000VND"'

# ── BIDV ──────────────────────────────────────────────────────────────────────
adb shell 'am broadcast -n com.app.docthongbaochuyenkhoan/.service.DebugReceiver --es pkg com.bidv --es title "BIDV +800,000 VND" --es text "TK:31410001234567 +800,000VND SD:4,500,000VND"'

# ── VPBank ────────────────────────────────────────────────────────────────────
adb shell 'am broadcast -n com.app.docthongbaochuyenkhoan/.service.DebugReceiver --es pkg com.vpbankonline --es title "VPBank +600,000 VND" --es text "TK:VPB123456 +600,000VND SD:3,000,000VND"'

# ── MoMo (parser riêng: tìm "So tien: X d" trong text) ──────────────────────
adb shell 'am broadcast -n com.app.docthongbaochuyenkhoan/.service.DebugReceiver --es pkg com.mservice.momotransfer --es title "MoMo" --es text "Ban da nhan tien. So tien: 300,000 d tu Nguyen Van A"'

# ── Shinhan (parser riêng: tìm "thay doi + VND X" trong text) ───────────────
adb shell 'am broadcast -n com.app.docthongbaochuyenkhoan/.service.DebugReceiver --es pkg com.shinhan.smartsalary --es title "Shinhan Bank" --es text "So du tai khoan thay doi + VND 500,000 luc 12:00"'

# ── Log ───────────────────────────────────────────────────────────────────────
adb logcat -s DebugReceiver MainActivity
